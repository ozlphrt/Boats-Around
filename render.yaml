services:
  - type: web
    name: boats-around-proxy
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node proxy-server.cjs
    envVars:
      - key: PORT
        value: 3001
